Flask==3.1.3
flask-cors==6.0.5
flask-talisman==1.1.0
flask-limiter==3.8.0
gunicorn==22.0.0
bcrypt==5.0.0
razorpay==1.4.2
psycopg2-binary==2.9.10
requests==2.33.0
biopython==1.87
primer3-py==2.3.0
tenacity==9.1.4
bleach==6.4.0
python-dotenv==1.2.2
celery[redis]==5.4.0
cryptography==44.0.2
setuptools==78.1.1
# pysam==0.23.3 removed — installed via wheel or apt in CI/CD deploy workflow

# ── Consensus Pipeline (ESMFold → Vina → GNINA) ─────────────────────────
# Heavy ML deps (torch, transformers, fair-esm, etc.) are in requirements-docking.txt
# installed ONLY on the Azure worker. Main server only needs rdkit + meeko for Vina prep.
rdkit>=2024.3.1
meeko>=0.7.1
