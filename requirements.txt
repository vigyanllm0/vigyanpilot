Flask==3.1.3
flask-cors==6.0.5
flask-talisman==1.1.0
flask-limiter==3.8.0
gunicorn==22.0.0
bcrypt==5.0.0
razorpay==1.4.2
# psycopg2-binary==2.9.10 — installed separately in deploy workflow (handles uv vs pip)
requests==2.33.0
biopython==1.87
primer3-py==2.3.0
tenacity==9.1.4
bleach==6.4.0
python-dotenv==1.2.2
celery[redis]==5.4.0
cryptography==44.0.2
setuptools==78.1.1
# pysam==0.23.3 removed — installed via wheel or apt in CI/CD deploy workflow

# ── Consensus Pipeline (ESMFold → Vina → GNINA) ─────────────────────────
# ESMFold — Protein structure prediction (MIT license)
# torch>=2.2.0 — installed separately via CPU index to avoid CUDA download on t3.micro
transformers>=4.40.0
tokenizers>=0.15.0
fair-esm>=2.0.0
einops>=0.6.0
accelerate>=0.26.0

# Cheminformatics & Docking
rdkit>=2024.3.1
meeko>=0.7.1
# vina>=1.2.5 — CLI binary installed via apt (autodock-vina); Python bindings only needed for Colab
