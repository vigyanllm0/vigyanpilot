# ── Heavy ML dependencies for Consensus Pipeline (ESMFold → GNINA) ─────
# Installed ONLY on Azure worker (GPU, sufficient disk). Skip on t3.micro.
torch>=2.2.0
transformers>=4.40.0
tokenizers>=0.15.0
fair-esm>=2.0.0
einops>=0.6.0
accelerate>=0.26.0
# vina Python bindings — only needed for Colab T4 server
# vina>=1.2.5
